{
  "name": "@methcreate/monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "compile": "yarn workspace @methcreate/hardhat compile",
    "fork": "yarn workspace @methcreate/hardhat fork",
    "test": "yarn workspace @methcreate/hardhat test",
    "coverage": "yarn workspace @methcreate/hardhat coverage",
    "deploy": "yarn workspace @methcreate/hardhat deploy",
    "start": "yarn workspace @methcreate/client start"
  }
}
